export interface expirationDateProps {
    date: string
    group: 'dairy' | 'meat' | 'vegetable' | 'fruit' | 'grain' | 'fat'
    // packaging: string
    // refridgerated?: boolean
    // frozen?: boolean
}